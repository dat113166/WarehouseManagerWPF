<UserControl x:Class="WMS.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Background="White" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Vertical" Width="320">
                <TextBlock Text="Sản phẩm"/>
                <ComboBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" DisplayMemberPath="ProductName"/>

                <TextBlock Text="Kho hàng" Margin="0,8,0,0"/>
                <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse, Mode=TwoWay}" DisplayMemberPath="WarehouseName"/>

                <TextBlock Text="Số lượng" Margin="0,8,0,0"/>
                <TextBox Text="{Binding StockQuantity, UpdateSourceTrigger=PropertyChanged}"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Thêm" Width="80" Margin="0,0,8,0" Command="{Binding AddCommand}"/>
                    <Button Content="Sửa" Width="80" Margin="0,0,8,0" Command="{Binding UpdateCommand}"/>
                    <Button Content="Xóa" Width="80" Command="{Binding DeleteCommand}"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Text=" " Width="16"/>

            <StackPanel Orientation="Vertical" VerticalAlignment="Top">
                <TextBlock Text="Danh sách tồn kho" FontSize="16" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Inventories}" AutoGenerateColumns="False" SelectedItem="{Binding SelectedInventory, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding InventoryID}" Width="50"/>
                <DataGridTextColumn Header="Sản phẩm" Binding="{Binding Product.ProductName}" Width="*"/>
                <DataGridTextColumn Header="Kho" Binding="{Binding Warehouse.WarehouseName}" Width="150"/>
                <DataGridTextColumn Header="Số lượng" Binding="{Binding StockQuantity}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>