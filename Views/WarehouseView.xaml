<UserControl x:Class="WMS.Views.WarehouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Background="White" Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Quản lý Kho" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="Tên kho" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Vị trí" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"/>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="Add" Width="80" Margin="0,0,8,0" Command="{Binding AddCommand}"/>
                <Button Content="Update" Width="80" Margin="0,0,8,0" Command="{Binding UpdateCommand}"/>
                <Button Content="Delete" Width="80" Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Column="1" ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse, Mode=TwoWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding WarehouseID}" Width="60"/>
                <DataGridTextColumn Header="Tên kho" Binding="{Binding WarehouseName}" Width="*"/>
                <DataGridTextColumn Header="Vị trí" Binding="{Binding Location}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>