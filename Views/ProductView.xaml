<UserControl x:Class="WMS.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F4F7FC">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="Danh sách Sản phẩm" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <DataGrid ItemsSource="{Binding ListProducts}" 
                          SelectedItem="{Binding SelectedProduct}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          Height="500"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="50"/>
                        <DataGridTextColumn Header="Tên Sản phẩm" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="Đơn vị" Binding="{Binding Unit}" Width="1*"/>
                        <DataGridTextColumn Header="Giá" Binding="{Binding Price, StringFormat='N0'}" Width="1*"/>
                        <DataGridTextColumn Header="Loại SP" Binding="{Binding Category.CategoryName}" Width="1*"/>
                        <DataGridTextColumn Header="NCC" Binding="{Binding Supplier.SupplierName}" Width="1.5*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10,0,0,0">
            <StackPanel>
                <TextBlock Text="Chi tiết Sản phẩm" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <TextBlock Text="Tên sản phẩm (*)" Margin="0,0,0,2"/>
                <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Text="Đơn vị tính" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Text="Giá (*)" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Text="Loại sản phẩm (*)" Margin="0,0,0,2"/>
                <ComboBox ItemsSource="{Binding ListCategories}"
                          SelectedItem="{Binding SelectedCategory}"
                          DisplayMemberPath="CategoryName"
                          Margin="0,0,0,10"/>

                <TextBlock Text="Nhà cung cấp" Margin="0,0,0,2"/>
                <ComboBox ItemsSource="{Binding ListSuppliers}"
                          SelectedItem="{Binding SelectedSupplier}"
                          DisplayMemberPath="SupplierName"
                          Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Thêm" Command="{Binding AddCommand}" Margin="5" Width="60"/>
                    <Button Content="Sửa" Command="{Binding UpdateCommand}" Margin="5" Width="60"/>
                    <Button Content="Xóa" Command="{Binding DeleteCommand}" Margin="5" Width="60"/>
                </StackPanel>
                <Button Content="Làm mới Form" Command="{Binding ClearFormCommand}" Margin="5,10,5,5" />

            </StackPanel>
        </Border>
    </Grid>
</UserControl>