<UserControl x:Class="WMS.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Background="White" Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Khách hàng" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="Họ Tên" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Địa chỉ" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Số điện thoại" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="Add" Width="80" Margin="0,0,8,0" Command="{Binding AddCommand}"/>
                <Button Content="Update" Width="80" Margin="0,0,8,0" Command="{Binding UpdateCommand}"/>
                <Button Content="Delete" Width="80" Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Column="1" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="60"/>
                <DataGridTextColumn Header="Tên" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="200"/>
                <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>