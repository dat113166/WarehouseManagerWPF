<Window x:Class="WMS.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WMS"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="30">
            <TextBlock Text="Tên đăng nhập:" />
            <TextBox x:Name="txtUsername" Margin="0,5,0,15" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="24"/>

            <TextBlock Text="Mật khẩu:" />
            <PasswordBox Name="PasswordBox" Margin="0,5,0,20" Height="24" />

            <Button Content="Đăng nhập"
                    Height="30"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PasswordBox}"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
